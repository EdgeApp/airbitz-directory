#!/bin/bash

FILE=solr-4.6.0
ARCHIVE=$FILE.zip
if [[ ! -f ${ARCHIVE} ]]; then
    wget http://archive.apache.org/dist/lucene/solr/4.6.0/${ARCHIVE}
    unzip ${ARCHIVE}
fi
cd $FILE/example/
mkdir -p <%=@home%>/solr
cp -a * start.jar <%=@home%>/solr/

